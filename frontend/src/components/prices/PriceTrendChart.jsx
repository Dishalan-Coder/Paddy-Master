import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, Legend } from 'recharts';
const C = {nadu:'#16a34a',samba:'#d97706',k_samba:'#2563eb'};
export default function PriceTrendChart({ trend }) { if(!trend?.length) return <p className="text-gray-400 text-sm py-8 text-center">No trend.</p>; const vars = Object.keys(C); const d = trend.map(e=>{const p={date:e.date?.slice(5)||''};vars.forEach(v=>{p[v]=e.prices?.[v]||null;});return p;}); return <div className="card"><h3 className="font-semibold mb-4">7-Day Trend</h3><div className="h-72"><ResponsiveContainer width="100%" height="100%"><LineChart data={d}><XAxis dataKey="date" tick={{fontSize:12}}/><YAxis tick={{fontSize:12}}/><Tooltip formatter={v=>v?`Rs. ${v}/kg`:'—'}/><Legend/>{vars.map(v=><Line key={v} type="monotone" dataKey={v} stroke={C[v]} strokeWidth={2} dot={{r:3}} connectNulls name={v.replace('_',' ').toUpperCase()}/>)}</LineChart></ResponsiveContainer></div></div>; }
